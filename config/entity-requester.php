<?php

return [
    /*
    | determine if schemas should be cached
    */
    'use_cache' => false,

    /*
    | determine in which directory shchemas are stored
    | by default, schemas are stored in the "schemas" directory at the root of the project
    */
    'schema_directory' => null,
];
