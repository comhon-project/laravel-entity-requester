<?php

return [
    /*
    | determine if schemas should be cached
    */
    'use_cache' => false,

    /*
    | determine in which directory entities schemas are stored
    | by default, files are stored in the "schemas/entities" directory at the root of the project
    */
    'entity_schema_directory' => null,

    /*
    | determine in which directory requests schemas are stored
    | by default, files are stored in the "schemas/requests" directory at the root of the project
    */
    'request_schema_directory' => null,
];
